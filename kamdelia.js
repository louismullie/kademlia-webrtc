var Kademlia = {
  
  config: {
    alpha: 3,
    k: 8,
    rpcTimeout: 5,
    iterativeLookupDelay: 2.5,
    refreshTimeout: 3600,
    replicateInterval: 3600,
    dataExpireTimeout: 86400,
    checkRefreshInterval: 720,
    udpDatagramMaxSize: 8192
  }
  
};